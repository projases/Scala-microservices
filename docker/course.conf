database {
  driver   = "org.postgresql.Driver"
  url      = "jdbc:postgresql://postgres:5432/course"
  user     = "demo"
  password = "demo"
  migrate  = true
}

server {
  host = "0.0.0.0"
  port = 18084
}

userService {
  baseUrl = "http://user:18082"
}

microcredentialService {
  baseUrl = "http://microcredential:18085"
}

retry {
  maxAttempts     = 3
  baseDelayMillis = 200
}

rabbit {
  enabled     = true
  host        = "rabbitmq"
  port        = 5672
  virtualHost = "/"
  username    = "guest"
  password    = "guest"
  exchange    = "course.events"
  queue       = "course.events.closed"
  routingKey  = "course.closed"
}
