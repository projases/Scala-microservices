database {
  driver   = "org.postgresql.Driver"
  url      = "jdbc:postgresql://localhost:54321/users"
  user     = "user"
  password = "user"
  migrate  = true
}

server {
  host = "0.0.0.0"
  port = 18082
}

productService {
  baseUrl = "http://localhost:18081"
}
