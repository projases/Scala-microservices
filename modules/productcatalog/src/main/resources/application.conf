server {
  host = "0.0.0.0"
  port = 18081
}

retry {
  maxAttempts     = 3
  baseDelayMillis = 200
}

rabbit {
  enabled     = true
  host        = "localhost"
  port        = 5672
  virtualHost = "/"
  username    = "guest"
  password    = "guest"
}
